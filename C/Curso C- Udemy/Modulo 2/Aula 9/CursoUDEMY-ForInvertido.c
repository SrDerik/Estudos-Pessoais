#include <stdio.h>

int main(){
     printf("Contagem regressiva para uma bomba explodir\n");
     
     //Utilizando o i-- para fazer um for invertido 
    for (int i = 10; i >= 0 ; i--){
        printf("%d\n", i);
    }

return 0;
}