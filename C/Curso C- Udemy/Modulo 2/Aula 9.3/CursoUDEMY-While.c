#include <stdio.h>

int main(){

    int contador=0;

    //While, faz tal ação até que uma condição seja atendida
     
    while (contador <= 5);
        printf("Contador:%d\n", contador);
        contador++;

    return 0;
}