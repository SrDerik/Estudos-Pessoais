#include <stdio.h>
#include <math.h>

int main(){
int base=2, expoente=3;

printf("2 elevado a 3 é igual a: %.2f", pow(base,expoente));
printf("Raiz de 2 é igual a: %.2f", sqrt(base));
printf("Modulo de 2 - 3 é igual a: %.2f", abs(base- expoente));

    return 0;
}